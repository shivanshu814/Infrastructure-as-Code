# Server Configuration
PORT=3000
NODE_ENV=development

# External API Keys (Get from AWS Secrets Manager in production!)
# NEVER commit real values - this is just an example file
OPENROUTER_API_KEY=your_openrouter_key_here
FAL_KEY=your_fal_key_here

# AWS Configuration (use IAM roles in production, not static keys!)
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_REGION=us-east-1

# Optional: Database connection (for future use)
# DATABASE_URL=postgresql://user:password@localhost:5432/dbname
